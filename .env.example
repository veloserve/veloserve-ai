# LLM provider credentials
# Set the provider keys you actually use before local runs or AMP deployment.
OPENAI_API_KEY=

# Optional model overrides
MODEL=

# Example extra variables for future tools/integrations
SERPER_API_KEY=
